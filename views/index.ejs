<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://fonts.googleapis.com/css?family=Inconsolata:400,700|Montserrat:300,400,700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/style.css" />
    <title>gitPub</title>
  </head>
  <body>
    <div class="body-overlay">
      <!-- ============ HEADER ============ -->
      <header>
        <h1>gitPub</h1>
        <ul>
          <li><a href="/pub">master branch</a></li>
          <li><a href="/new">git add -an item</a></li>
        </ul>
      </header>
      <!-- ============ MAIN ============ -->
      <main>
        <div>
          <h1>fooBar()</h1>
          <ul>
            <% for( let i = 0; i < drinks.length; i++ ) { %>
            <li><a href="/drinks/<%=i%>"><%= drinks[i].name %></a></li>
            <% } %>
          </ul>
        </div>
        <div>
          <h1>fooD()</h1>
          <ul>
            <% for( let i = 0; i < food.length; i++ ) { %>
            <li><a href="/food/<%=i%>"><%= food[i].name %></a></li>
            <% } %>
          </ul>
        </div>
      </main>
      <!-- ============ FOOTER ============ -->
      <footer>
        <span>℗ 2022</span>
        <span>gitPub Food Corp.</span>
      </footer>
    </div>
  </body>
</html>
